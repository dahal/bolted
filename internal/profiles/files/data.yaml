# data — data engineering / analytics starter.
#
# gh + gcloud (gcloud bundles `bq` for BigQuery), plus apk packages for
# local data wrangling.
features:
  - ghcr.io/devcontainers/features/github-cli:1
  - ghcr.io/devcontainers/features/gcloud-cli:1
packages:
  - jq
  - duckdb
  - python3
