# minimal — base image only.
#
# Provides nothing beyond what the base Alpine VM image ships with
# (podman, cryptsetup, git, openssh, bash, curl). Use this as a starting
# point and add features / packages as you need them.
features: []
packages: []
